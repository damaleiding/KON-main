# 2026-05-30 外层框架清理清单

## 清理目标

- 统一当前项目路径名，避免 `projects/novel` 和 `projects/pixiv novel` 混用。
- 删除最外层主干目录中的一次性临时脚本、乱码 HTML、旧评估结果。
- 删除确认为空且无索引价值的空目录。
- 保留正文、设定、审稿、账本和备份记录，避免误删有用迭代事实。

## 已修正路径

- `README.md`：将后续整理目标中的 `projects/novel/` 改为 `projects/pixiv novel/`。
- `projects/pixiv novel/AGENTS.md`：将项目定位路径改为 `projects/pixiv novel`。
- `projects/pixiv novel/docs/current-layout.md`：将当前目录映射路径改为 `projects/pixiv novel`。
- `tools/word-count/README.md` 和 `tools/word-count/word-count.ps1`：示例路径改为带引号的 `projects/pixiv novel` 路径。
- `projects/pixiv novel/90_工具/replace_script.py`：移除旧本机绝对路径，改为当前项目相对路径。
- `projects/pixiv novel/_pipeline/tool-index.json`：项目名改为 `pixiv novel`，并登记迁移后的 `split_lemon_cards.py`。

## 已删除根目录临时文件

- `temp_*.py`：一次性章节修复、检查、重写、HTML 更新脚本。
- `temp_claude_review_prompt.md`：一次性 Claude 复检提示词。
- `page.html`：乱码 HTML 临时输出。
- `clean_merged.py`、`recount_merged.py`、`recount_merged2.py`：一次性章节合并清理和字数重算脚本。
- `imagine-cli-static-eval-result.json`、`imagine-cli-trigger-eval-result.json`、`imagine-cli-trigger-eval-result-v2.json`：旧 Imagine CLI 评估结果。

## 已移动根目录项目脚本

- `split_lemon_cards.py` 移动到 `projects/pixiv novel/90_工具/scripts/split_lemon_cards.py`。

## 已删除空目录

- `projects/pixiv novel/.trae/skills/曲影故事润色`
- `projects/pixiv novel/柠檬/发布`
- `projects/pixiv novel/曲影/发布`
- `projects/pixiv novel/曲影/分支`
- `projects/pixiv novel/人偶番外/归档`

## 保留项

- `tokens/`：本地 Google Docs / clasp 等凭据目录，已在 `.gitignore` 中排除，不进入版本库。
- `.env`：本机配置，已在 `.gitignore` 中排除。
- `projects/pixiv novel/_backups/`：正文修改前备份，属于可追溯安全层，不清理。
- `projects/pixiv novel/_ledger/` 和 `review/`：迭代账本和审稿报告，属于项目事实层，不清理。
- `.gitkeep`：用于保留必要空目录，例如 `methods/`、`references/`、`templates/`、项目 `refs/`、`prompts/`、`outputs/`。

## 后续候选

- 如果确认不再需要历史正文备份，可另行按日期清理 `projects/pixiv novel/_backups/`，但清理前应先确认对应修改已经稳定。
- 如果不再使用旧 `90_工具/replace_script.py`，建议先迁移成参数化工具，再删除硬编码版本。
