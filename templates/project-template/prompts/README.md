# Prompt 入口

保存可复用 prompt、角色 prompt、风格 prompt、续写 prompt、润色 prompt 和审稿 prompt。

每个重要 prompt 应说明目标、模型、参考输入、限制条件、版本差异和预期用途。

生成或修改重要 prompt 时，按 `docs/agent-rules/prompt-generation.md` 执行；可从工作区 `templates/prompt-run-template.md` 复制记录模板。
