# 文本整理规则模板

## 目录边界

- `原文/`：默认只读。
- `设定/`：稳定事实和作品级规则。
- `主干/`：认可正文。
- `分支/`：实验和替代路线。
- `发布/`：对外整理版本。
- `_ledger/`：生产事实和版本流转。
- `review/`：审稿记录。
- `_tmp/`：临时文件。

## 整理规则

- 移动正文前先生成迁移表。
- 修改正文前先备份。
- 拆分、合并、发布整理后更新章节索引、字数统计和必要账本。
- 不把长规则、长词表、prompt 长文塞进入口文件。
