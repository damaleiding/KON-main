# 来源说明

## 当前文本

| 文件 | 来源 | 导入时间 | 编码 | Git 状态 |
| --- | --- | --- | --- | --- |
| `断龙台.txt` | 用户提供的本地 TXT | 2026-06-28 | GBK/GB2312 | 默认不进入 Git |
| `邪兵谱.txt` | 用户提供的本地 TXT | 2026-06-28 | UTF-8 | 默认不进入 Git |

## 使用边界

- 这两个 TXT 只作为本机全文级结构研究资料。
- 分析输出可以写入 `analysis/`，但不要在分析文件中复制大段原文。
- 后续引用时只做短摘录、概述、章节结构表和技法提炼。
- 本目录 `.gitignore` 已排除 `*.txt`，避免全文被默认提交。

## 统计口径

字数检查使用工作区工具 `tools/word-count/word-count.ps1`。

- 默认口径：非空白 Unicode 字符数。
- `断龙台.txt` 使用 `-Encoding gb2312`。
- `邪兵谱.txt` 使用 `-Encoding utf-8`。
